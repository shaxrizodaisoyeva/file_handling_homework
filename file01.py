def main(data:str):
    """
    The data is from the file. Return data as a list type.
    Args:
        data: str
    Returns:
        list: return answer
    """
    a=data.split(",")
    b=list(a)
    return a 
# Read data from file